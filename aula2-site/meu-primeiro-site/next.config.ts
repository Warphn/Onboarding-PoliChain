// next.config.ts  — versão “default”

/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode já vem ativado por padrão no App Router.
  // Adicione opções aqui quando precisar.
};

export default nextConfig;
